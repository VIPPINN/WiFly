import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('WiFly', () => App);
AppRegistry.runApplication('WiFly', {
  rootTag: document.getElementById('root'),
});
